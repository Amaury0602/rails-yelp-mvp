<h1><%= @restaurant.name %> reviews</h1>
<h2><%= @restaurant.category %></h2>

<p>Write your review and rate your experience</p>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating%>
  <%= f.submit "Submit your review", class: "btn btn-primary" %>
<% end %>

<%= link_to "Go back to restaurant information", restaurant_path(@restaurant) %>

